# Frontend Environment Variables
# CDN XData by Cognition Labs EU

# API Base URL
# Leave empty ('') for same-domain deployment (proxy setup)
# Set to backend URL for separate frontend deployment (e.g., Netlify)
# Examples:
#   VITE_API_BASE_URL=                          # Same domain (default)
#   VITE_API_BASE_URL=https://cdn.xdata.si     # Production backend
#   VITE_API_BASE_URL=http://localhost:3000    # Local development

VITE_API_BASE_URL=
